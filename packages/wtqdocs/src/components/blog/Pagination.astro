---
import { Icon } from 'astro-icon/components'
import { usePagination } from 'astro-toolkit/utils'

const { length, currentPage, firstUrl, prevUrl, nextUrl } = Astro.props

const pagination = usePagination(currentPage, length)
---

<nav aria-label="Blog pages" class="wtq-pagination">
  <a href={prevUrl} class:list={["wtq-pagination__link", { disabled: prevUrl }]}>
    <Icon class="wtq-first-arrow" name="arrow" />
  </a>
  {
    pagination.map((page) => (
      page ?
      <a
        class:list={[
          "wtq-pagination__link",
          { disabled: 1 == page },
          { active: page === currentPage },
        ]}
        href={1 == page ? firstUrl : `${firstUrl}/${page}`}
      >
        {page}
      </a> : <span
        class:list={[
          "wtq-pagination__link",
        ]}
      >
        ...
      </span>
    ))
  }
  <a href={nextUrl} class:list={["wtq-pagination__link", { disabled: nextUrl }]}>
    <Icon name="arrow" />
  </a>
</nav>
